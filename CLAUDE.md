# 콘티 — 교회 찬양팀 악보 앱

**현재 상태:** `index.html` 하나. v48. `https://gcode77.github.io/konti/` 에 배포됨.
**저장소:** `github.com/GCODE77/konti` (public), 브랜치 `main`, 파일명 반드시 `index.html`.

## 지금 당장 할 것

1. 이 폴더를 열고 `index.html`을 그대로 브라우저로 열어 동작 확인
   (사진 인식은 https 환경에서만 됨 — CORS. 로컬 파일로 열면 그 기능만 막힘)
2. **분리 작업 시작.** 2184줄 단일 `<script>`를 아래 "분리 계획"대로 쪼갤 것
3. 분리 후에도 **GitHub Pages 배포는 `index.html` 파일 하나 여야 함** — 빌드 스텝을
   넣는다면 반드시 결과물을 `index.html`로 다시 합치는 빌드 파이프라인을 구성할 것.
   합치지 않고 여러 파일로 쪼갠 채로 두면 Pages가 그대로 서빙하지 못함(모듈 로더 없음).

## 이게 뭔가

교회 반주자가 **종이 악보(코드 차트 또는 오선보) 사진을 찍으면**:
- 코드·가사·멜로디를 텍스트로 변환 (Gemini 무료 API 또는 Claude 유료 API)
- 키를 자유롭게 조옮김 (코드+멜로디 동시)
- 장르별 반주로 재생 (관현악 합성 엔진, 11개 악기 동시 연주 가능)
- 악보 형태(오선보+코드+가사)로 화면에 표시, 음표를 눌러 직접 수정
- **올린 사진을 그대로 띄우고 재생 위치를 사진 위에 하이라이트로 표시**(v48) —
  인식이 못 미더울 때 결국 반주자가 보는 건 원본이다. 아래 6번 항목 참고
- PDF/PNG/TXT로 내보내기, 인쇄
- 서버 없이 GitHub Pages 정적 호스팅 하나로 전부 동작

## 왜 이런 구조인가 — 바꾸기 전에 읽을 것

1. **오선보 OMR을 하지 않고 코드 차트 변환에 집중했다가, 결국 오선보까지 갔다.**
   처음엔 "코드는 규칙 연산이라 정확도 100%"라는 이유로 코드 차트만 다뤘다.
   그런데 사용자가 실제로 필요한 게 오선보(멜로디 포함)였고, Gemini Vision이
   생각보다 오선보를 잘 읽어서 멜로디까지 확장했다. **다만 멜로디 음높이 인식은
   여전히 부정확할 수 있다** — 그래서 오선보 편집 UI(음표 클릭→반음/옥타브 수정)가
   핵심 기능이 됐다. 인식이 완벽할 거라 기대하지 말 것.

   v47에서 음높이 정확도를 네 방향으로 손봤다. **완벽해지지 않는다는 전제는 그대로다** —
   목표는 "덜 틀리게 + 틀린 데를 빨리 찾게" 였다.
   a. **단 분할이 실제로는 작동하지 않고 있었다(가장 큰 원인).** "단 단위로 자르면
      정확해진다"며 만든 `splitSystems` 가 `prepImage` 의 **1600px로 줄인 사본**에서
      잘라내고 있었다. 실제 찬송가 한 쪽(오선 12단)을 넣어 재 보니 1600px 사본에서는
      오선을 **한 단도 못 찾고**(0단) 그냥 저해상도 통짜 한 쪽을 그대로 보냈다.
      3000px 사본을 따로 만들어 그걸 자르게 하니 **12단을 다 찾고** 단마다 1800px 폭으로
      보낸다. 음높이 인식은 "음표 머리가 어느 줄·칸에 놓였나"를 보는 일이라 이게 곧 정확도다.
      ★ 앞으로 `splitSystems` 에 넘기는 이미지가 `hiUrl` 인지 반드시 확인할 것.
   b. **프롬프트에 오선 자리표를 표로 박아 넣었다.** 예전엔 "음이름 + 옥타브 숫자"만
      요구해서 모델이 자리를 세는 단계를 건너뛰고 짐작했다. 지금은 자리표 확인 →
      음표 머리가 줄인지 칸인지 → **아래에서부터 세어 표에서 찾기** → 덧줄 개수 세기 →
      조표 반영 → 검산(같은 자리는 같은 음이름 / 옥타브 도약이 보이면 다시 세기) 순서를
      강제한다. 기둥 방향은 음높이와 무관하다는 것도 명시했다(자주 헷갈린다).
      단이 바뀔 때는 **앞 단의 마지막 음**을 알려 줘서 옥타브 기준으로 쓰게 한다.
   c. **옥타브 오독은 규칙으로 고친다(`repairOctaves`).** 음이름은 맞게 보고 옥타브만
      틀리는 게 가장 흔한 오류다. 비터비로 "옮기면 벌점 / 5도 넘는 도약에 벌점 /
      음역 이탈에 벌점"을 최소화해 **음이름은 건드리지 않고 옥타브만** 옮긴다.
      진짜 옥타브 도약을 망치지 않는 게 핵심이라 **양 끝 음은 음역을 벗어나지 않는 한
      건드리지 않는다**(이웃이 한쪽뿐이어서 "혼자 튀었다"는 증거가 없다 —
      `[60,72,74,…]` 를 `[72,72,74,…]` 로 망치는 걸 테스트가 잡았다).
      절반 넘게 고쳐야 하면 가정 자체가 틀린 것이므로 손대지 않는다.
   d. **틀렸을 법한 음을 화면에 동그라미로 표시한다(`markSuspectNotes`).**
      조성·코드에 없는 음(임시표 오독), 옥타브 넘는 도약, 우리가 옥타브를 고친 음.
      **3도·2도 차이로 잘못 읽은 음은 이 방법으로 못 잡는다** — 조성 안에 그대로
      들어맞아서 규칙으로는 구분이 안 된다. 그건 재생해서 귀로 확인하는 수밖에 없다.
      이 한계를 사용자에게 숨기지 말 것.
   또 **채보 결과를 스스로 채점해(`melScore`) 이상한 단만 다시 읽는다.** 마디 박수가
   박자표와 안 맞거나 음역 이탈·큰 도약이 있으면 그 단만 한 번 더 읽고 더 나은 쪽을 쓴다.
   전부 두 번 읽지 않는 이유는 Gemini 무료 한도를 두 배로 쓰기 때문이다 — 사용자가
   "유료면 절대 안 된다"고 한 제약이 여기까지 영향을 준다.

2. **서버를 두지 않기로 결정했다.** 사용자가 배포/서버 운영에 익숙하지 않아
   GitHub Pages(정적 호스팅, 무료) + Gemini API(무료 티어) 조합으로 감. 이 구조를
   깨고 백엔드를 넣으려면 사용자에게 새로 설명해야 함 — 가볍게 결정하지 말 것.

3. **Gemini가 기본, Claude는 옵션.** 사용자가 "유료면 절대 안 된다"고 명시했다.
   Gemini 무료 티어(하루 한도 있음, 태평양 시간 자정 리셋)가 기본 엔진.
   `thinkingConfig:{thinkingBudget:0}` 필수 — 안 넣으면 내부 추론에 출력 토큰을
   다 써서 결과가 잘린다. 실제로 겪은 버그.

4. **경쟁자는 CCLI SongSelect.** 이미 한국 진출했고 조옮김·코드차트를 제공함.
   이 앱의 존재 이유는 "SongSelect 카탈로그에 없는 종이 악보"를 다루는 것.
   저작권: 은혜(손경민 작곡) 등 실제 곡 가사/멜로디를 AI가 대신 옮겨 적어주는 건
   하지 않기로 함 — 사용자의 악보 업로드→변환은 괜찮지만, 어시스턴트가 곡을
   직접 받아 적는 건 별개 문제로 선을 그었다.

5. **PDF는 이미지 기반.** 외부 라이브러리 없이 캔버스→JPEG→PDF 바이트를 직접
   조립한다. 텍스트 검색은 안 되지만 한글 폰트 임베딩 문제를 피함.

6. **"원본 사진 보기"가 인식 실패의 최종 안전망이다(v48, 사용자 요청).**
   OCR 폴백 결과("다중 노 즈즈 즈 드…")를 받아 본 사용자가 직접 요청했다 —
   *"OCR로만 표기하지 말고 차라리 내가 올린 사진을 그대로 비춰주면서 사진 위에
   하이라이트로 재생을 따라가게 해 달라. 그게 더 효과적이고 가시성도 좋다."*
   맞는 판단이다. 인식이 아무리 나빠도 **원본 사진은 항상 100% 정확한 악보**이고,
   앱이 줄 수 있는 값은 "지금 어디를 연주하는지"뿐이다.
   - `S.orig = {pages:[{url, sys:[{t0,t1,b0,b1,n}]}], approx}`
     `t0/t1` = 그 단이 사진에서 차지한 세로 구간(0~1) · `b0/b1` = 곡 전체 진행 구간(0~1)
   - 단 위치는 `splitSystems` 가 이미 찾아낸 오선 띠를 재활용한다(공짜로 얻는 정보다).
   - 단 길이는 `sysWeight` 로 그 단 텍스트의 박수를 재서 비율로 나눈다. 재생 엔진의
     계산과 완전히 같지는 않다 — **눈으로 따라가는 보조 표시라 한 마디 오차는 괜찮다.**
     정확히 맞추려고 파서에 줄 번호를 심는 건 하지 말 것(사용자가 텍스트를 고치면 깨진다).
   - **OCR 폴백일 때도 사진은 보여 준다.** 단 구분 정보가 없으니 쪽 전체를 한 덩어리로 잡는다.
   - 사진은 `showUrl`(1400px, q .72)로 따로 만든다. 보내는 사본(`url`)은 흑백·고대비라
     사람이 보기엔 원본이 낫다.
   - localStorage 는 '마지막 상태' 한 곡에만 사진을 넣고, 한도를 넘기면 **사진만 빼고
     다시 저장한다** — 사진 때문에 곡 자체를 못 저장하면 훨씬 큰 손해다.

## 실제로 겪은 버그들 (재발 방지용 메모)

- **오디오 엔진 핵심부(`AC()`, `mtof`, `pluckBuf`, 리버브 초기화, `PATTERNS` 반주
  패턴표)를 통째로 실수로 지운 적 있음.** 문법 오류 없이 "재생 버튼이 눌리는데
  아무것도 안 남" 형태로 나타났다 — 문법 검사만으로는 못 잡음.
  **반드시 실제 DOM/AudioContext를 흉내낸 스텁으로 주요 함수(togglePlay,
  openPanel 각 패널, saveSong, seekTo 등)를 실행까지 해보고 배포할 것.**
  이 저장소에는 그런 테스트 스크립트가 없다 — 새로 만들어 CI에 넣을 것을 권장.
- **`[Chorus]`, `[Bridge]` 같은 구간 이름이 코드로 오인식된 적 있음** (C+horus).
  코드 접미사 허용 문자를 화이트리스트로 제한해서 해결(`SUFFIX_RE`).
- **가사에 쉼표 기호 `r`이 새어 들어간 적 있음.** 정리 함수(`cleanLyric`)를 만들고도
  결과를 안 쓰고 원본을 그대로 쓰는 실수를 한 번 더 함 — 데이터 흐름을 끝까지
  추적할 것.
- **실제 찬송가 사진을 넣으니 화면이 못 쓸 만큼 읽기 어려웠던 것(v47에서 고침).**
  원인이 다섯 개 겹쳐 있었고, 그중 셋은 렌더가 아니라 **파서**였다.
  1. `2)` 절 가사 줄을 붙일 대상을 찾을 때 "코드가 하나라도 있는 pairs 블록"만
     인정했다. 둘째 단 이후는 코드가 안 붙는 일이 흔해서, 코드 없는 줄을 건너뛰고
     훨씬 앞의 다른 줄에 붙어 **그 줄의 2·3·4절 가사를 덮어써 버렸다.**
     실측: 105음절 악보에서 31음절(30%)이 화면에서 사라졌다. 지금은 (멜로디·빈 줄만
     건너뛰고) 바로 앞 pairs 블록에 붙이고, 같은 절이 이미 있으면 이어 붙인다.
  2. 오선보 렌더가 음절이 음표보다 많으면 `Math.min(syl.length, realCx.length)` 로
     **넘치는 음절을 잘라 버렸다.** 지금은 음표 위치를 기준선으로 두고 사이에
     끼워 넣어 하나도 버리지 않는다.
  3. AI가 긴 `♪` 멜로디 줄을 중간에서 줄바꿈하면 그 조각을 그냥 버려서 그 단의
     멜로디가 통째로 사라지고 빈 오선만 남았다. 지금은 앞 mel 블록에 이어 붙인다.
  4. 마디 폭을 음표 개수만으로 정해 오선이 화면 폭의 절반만 쓰고, 가사를 음표 중심에
     그대로 찍어 한글끼리 겹쳤다. 지금은 조판 순서를 따른다 — 마디마다 필요한 폭을
     먼저 재고(가사·코드 **글자 폭 포함**) → 행에 채우고 → 남는 폭을 비례 배분한다.
     한 행의 논리 폭은 `화면 폭 ÷ 확대율`로 잡는다. 예전엔 1000 고정이라 휴대폰에서
     글자가 3분의 1로 줄어 아예 못 읽었다.
  5. 가사 줄 높이가 `bottom+24` 고정이라 아래로 뻗은 기둥·깃발·덧줄과 겹쳤다.
     지금은 음표 잉크가 실제로 닿은 세로 범위를 재서 그 아래에 놓는다.
  또 **낮은음자리표를 그릴지 판단할 때 구간 전체를 봐서**(`bassM.some(...)`) 베이스
  음표가 없는 행에도 빈 오선을 그렸고, 멜로디가 없는 행에도 빈 오선 다섯 줄을 그렸다.
  둘 다 그 행에 음표가 있을 때만 그린다.
  **교훈: "가사가 안 나온다"는 신고를 렌더 문제로만 보지 말 것 — 절반이 파서였다.**
- **진단(diag) 패널이 `♪` 줄만 있으면 무조건 터졌다(v47에서 고침).**
  `parseMelody` 는 음표 **배열**을 돌려주는데 `mel.notes` 로 읽고 있었다.
  정작 멜로디-코드 불일치를 진단하려고 만든 화면이 그 상황에서만 죽던 셈.
- **인식이 실패해도 화면에 아무 말이 없었다(v48에서 고침). 가장 나쁜 종류의 버그였다.**
  성공/실패 메시지를 '가져오기' 패널 안의 `#scanstate` 에만 썼는데, 이 앱은 **패널을 닫아도
  인식이 백그라운드로 계속되도록 일부러 만들어 놨다.** 그래서 패널을 닫아 둔 사이에
  실패하거나 OCR로 대체되면 `clearWork()` 로 진행 표시만 사라지고 **끝났는지 실패했는지
  알 방법이 전혀 없었다.** 사용자가 실제로 이걸 겪고 "완료되었는지 안되었는지 알 수가
  없다"고 신고했다. 지금은 패널 밖 `#result` 배너에 띄우고, **누를 때까지 사라지지
  않는다**(`toast` 는 2.4초 뒤 없어져서 놓치면 끝이다). 성공·OCR대체·실패 세 상태를
  모두 알리고, 실패에는 다음 행동 버튼(다시 시도/직접 입력)을 붙인다.
  ★ 앞으로 백그라운드로 도는 작업을 추가하면 **결과 알림도 패널 밖에** 둘 것.
- **재생 화면 갱신이 `requestAnimationFrame` 하나에만 걸려 있었다(v48에서 고침).**
  RAF 는 탭이 뒤로 가거나 절전·저사양 상태에서 아예 멈춘다 — 그러면 **소리는 계속 나는데
  시간 표시·코드 하이라이트가 얼어붙는다.** 실제로 한 환경에서 RAF 가 1초에 **0번** 불리는
  것을 확인했고, 그래서 사진 하이라이트가 전혀 안 움직였다(코드는 정상이었다).
  지금은 갱신을 `tickUI()` 로 빼내 RAF 와 `setInterval(110ms)` 양쪽에서 돌린다.
  두 번 불려도 문제없게 만들어 뒀다. **재생 중 화면 표시를 추가할 때는 `tickUI` 에 넣을 것.**
- **CSS 클래스 이름이 기존 것과 충돌했다(v48).** 새로 만든 재생선에 `.sheethead` 를 썼는데
  이미 패널 머리글에 쓰이는 이름이었다. 뒤에 나오는 기존 규칙이 이겨서 `display:none` 이
  `display:flex` 로 덮였고, 꺼져야 할 재생선이 모든 단에 남았다. 새 클래스는 접두사를
  붙이고(`orig*`), **추가 전에 반드시 그 이름이 이미 있는지 확인할 것.**
- **멜로디가 코드와 타이밍이 안 맞은 적 있음.** 원인은 못갖춘마디(pickup measure)와
  단위 오차 누적. 마디 단위로 재동기화하고, 마디 길이에 강제로 맞추는
  옵션(`melFit`)을 추가해 해결.

## 배포 전 확인 (v48에서 실제로 돌려 본 것)

브라우저 콘솔에 붙여 넣어 한 번에 훑는 방식으로 확인했다. CI는 아직 없다.
- 렌더 경로: 악보/가사중심 모드, 이조 −12~+12, 넘버 표기, 카포, 확대 0.5~2,
  ♭ 강제, 장르 5종, 오선보 편집 화면(`renderStaff`)
- 시퀀서: `buildTimeline` 이벤트·멜로디·베이스 개수, `seekTo`, `togglePlay`
- 패널 8개(`scan input library export view diag play staff`) 열고 닫기
- 저장/복원/내보내기: `saveSong`, `saveLast`, `chartText`, `chartCanvas`,
  `pdfBytes`(결과가 `%PDF-1.4` 로 시작하는지)
- **가사 누락 검산(이번 버그의 회귀 테스트로 반드시 유지):** `S.raw` 의 가사 줄에서
  한글 음절을 세고, 렌더된 SVG `<text>` 의 음절 수와 대조해 **누락 0** 인지 본다.
  v46에서는 105음절 중 31개가 빠졌고 v47에서는 0개다. 화면만 눈으로 보면 못 잡는다.
- **음높이 보정 회귀 테스트(반드시 유지).** `repairOctaves` 는 "고쳐야 하는 것"보다
  **"건드리면 안 되는 것"이 더 중요하다.** 최소 케이스: 한 음만 옥타브 위/아래로 튄 것,
  음역을 통째로 벗어난 단, 베이스 보표 / 그리고 절대 안 건드려야 하는 것 — 순차 진행,
  5도 도약 반복, 진짜 옥타브 도약 후 그 높이 유지, 넓은 아르페지오, 음이 하나뿐,
  같은 음 반복, 양 끝 음. 18개 케이스를 통과시켜 두었다(대화 기록 참고).
  `writeBackMelody` → `reparse` 를 두 번 돌려도 음이 더 이상 바뀌지 않는지(왕복 안정성)도
  함께 볼 것 — 보정이 원문에 다시 써지므로 수렴하지 않으면 누를 때마다 음이 밀린다.
- **단 분할 해상도 검산.** 실제 찬송가 비율(오선 12단, 오선 한 칸이 쪽 높이의 0.55%)
  합성 이미지를 만들어 `splitSystems(p.url)` 과 `splitSystems(p.hiUrl)` 을 비교한다.
  저해상도 쪽이 0단을 돌려주면(=예전 상태) 단 분할이 죽은 것이다.
- **결과 알림 검산.** `closePanel()` 로 패널을 닫아 둔 상태에서 `showResult('err',…)` 를
  불러 배너가 실제로 보이는지(`getComputedStyle(#result).display==='block'`) 확인한다.
  패널 안 `#scanstate` 만 보고 "메시지 나온다"고 판단하면 안 된다 — 그게 이번 버그였다.
- **재생 하이라이트는 RAF 없이도 도는지 확인.** `requestAnimationFrame` 이 0번 불리는
  환경이 실제로 있다. `startPlay()` 후 1초 간격으로 `#chart .origsys.on` 라벨과
  `#time` 텍스트가 바뀌는지 본다. 안 바뀌면 `tickUI` 타이머가 죽은 것이다.
- **사진 하이라이트 매핑 검산.** 진행도 0/0.3/0.6/0.99 를 `paintSheet` 에 직접 넣어
  **항상 정확히 한 단만** `.on` 인지, 꺼진 단의 `.origline` 이 `display:none` 인지 본다
  (클래스 이름 충돌을 이 검사로 잡았다).

## 코드 구조 (분리 전 현재 상태)

`<script>` 안에 전부 있음. 대략적인 영역 순서:
1. 음악 이론 (파싱, 코드 표기, 조표, `spell()` 오선 위치 계산)
2. 차트 파서 (`parseChart`, `parseMelody`, 절 분리 `pairsForVerse`)
3. 오디오 엔진 (`AC`, `INSTRUMENTS` 11종, `PATTERNS` 반주 8종, `voice`, `strike`)
4. 시퀀서 (`buildTimeline`, `schedule`, 재생/절 자동 진행)
5. 오선보 렌더링 (`leadLineSVGs`, `renderStaff`, `spell`, `keySig`)
6. OCR (Tesseract.js, 키 없이 코드/가사만) — 오선보에는 정확도 낮음, 폴백용
7. Gemini/Claude 비전 API 호출 (`geminiScan`, `runScan`)
8. UI 렌더링 (`renderChart`, `renderToolbar`, 패널 7개: scan/input/library/
   export/view/diag/play)
9. 저장/복원 (`localStorage`, 자동 저장 `scheduleSave`)

## 분리 계획 (권장)

```
src/
  lib/
    music.js       — 음이론 상수, parseChord, showChord, keySig, spell
    parseChart.js  — parseChart, parseMelody, detectKey, pairsForVerse
    audio.js        — AC, INSTRUMENTS, PATTERNS, voice, strike, tapChord
    sequencer.js    — buildTimeline, schedule, startPlay/stopPlay, seekTo
    staff.js        — leadLineSVGs, renderStaff (SVG 오선보)
    ocr.js          — Tesseract 경로 (stripStaff, wordsToChart)
    vision.js       — geminiScan, runScan, makePrompt
    pdf.js          — buildPdf, canvasToPdfBytes, chartCanvas
    store.js        — localStorage 래퍼, 자동 저장/복원
  components/       — 패널 7개, 도구막대, 헤더
  main.js
index.html
```

**테스트부터 넣을 것.** `lib/music.js`와 `lib/parseChart.js`가 이 앱의 유일한
"정확도 100%" 주장(코드 이조)이 걸린 부분. 최소 케이스: 슬래시 코드, 이명동음
왕복, 12반음 왕복, sus/add/maj7, 마이너 감지, 마디선/못갖춘마디, 구간명이
코드로 오인되지 않는지.

## 검증해야 할 것 (코드보다 중요, 이전 세션에서 넘어온 과제)

- 반주자 3~5명 실사용 테스트 — 아직 안 됨
- 사진 변환 손익분기 (몇 곡 만에 손으로 고치는 게 빠른가)
- 주간 재사용률 — 유일한 진짜 성공 지표
- CCLI 코리아 라이선스 경로 문의 — 안 함
- Gemini 무료 티어 하루 한도가 실사용에 얼마나 자주 걸리는지

## 배포

```bash
# GitHub 저장소에 index.html 하나만 올리면 Pages가 자동 서빙.
# Settings → Pages → Branch: main / (root) 로 이미 설정됨.
```
