# session-handoff.md — 콘티(konti) v75 인계 문서

작성일: 2026-09-22 (v74 인계 문서를 그대로 이어받은 새 세션. v74가 "다음 세션이 이어받을
것 1번"으로 남긴 것 — 「하나님의 부르심」1단 픽업의 `headDur` 리듬 오독 — 을 이번 세션에서
실제로 찾아 고쳤다. 자세한 내용은 CLAUDE.md 80번 항목.)

마지막 커밋: 아직 없음 — `index.html`·`CLAUDE.md`가 **아직 커밋 안 됨**(v74분+이번
v75분 전부). 사용자에게 커밋 여부를 물어볼 것.

## ★★★★★★★ v75 — v74가 남긴 다음 할 일 1번을 고쳤다

CLAUDE.md 80번 항목에 전부 적어 뒀다 — 요약만 남긴다.

「하나님의 부르심 A코드.png」(사용자가 `악보/` 폴더에 넣어 둔 사진 — 앱에는 없어서
이번엔 `_tmp1.png`로 콘티 폴더에 잠깐 복사해서 재현하고 **끝나고 지웠다**) 1단(진짜
첫 마디, 못갖춤)의 픽업 두 음이 16분음표(0.25박씩, 사진 그대로)인데 `headDur`가
8분음표(0.5박씩, 2배)로 읽고 있었다.

- 원인: `gapTol`(빔 판정에서 작은 끊김을 이어 붙이는 허용 폭 — 원래 "지워진 오선 줄이
  빔을 두 조각으로 끊는 것"을 다시 잇기 위한 것)이 **길이만** 보고 있었는데, 이 사진에서는
  **진짜 빔 두 겹 사이의 빈틈이 우연히 `gapTol`과 똑같은 길이(3행)**로 나와서 지워진
  오선 줄 틈과 구별이 안 됐다 — 두 겹이 하나로 붙어(bands 2→1) 16분음표가 8분음표가 됐다.
- 고침: 틈을 이을지 말지에 **자리(`isL`, 오선 줄이 지나가는 행인가)** 조건을 더했다.
  지워진 오선 줄 틈은 반드시 그 줄이 지나가는 자리에서 생기고, 빔 두 겹 사이 틈은
  오선 줄과 무관한 자리에 생긴다 — 이 사진의 문제 틈은 그 자리가 셋 다 아니었다.
- **되돌려서 실측함(falsify)**: 이 조건을 끄면(옛 동작) 이 시스템의 **음표 20개 전부**가
  똑같이 8분음표(0.5박)로 뭉개졌다 — `durSure:true`(모두 같아서 자기모순이 없어 보임)
  였지만 명백히 틀린 확신이었다. 고친 뒤: 20음 중 18음이 사진과 정확히 일치·확신하고,
  나머지 2음은 스스로 `sure:false`로 남는다(아래 남은 일 참고, 급하지 않음).
- **실제 앱 경로(`prepImage`→`runOCR`→`addGeomMelody`)로도 확인함**: `S.raw`의 그 픽업이
  `♪ C#4:0.25 D4:0.25 | ...` 로 정확히 바뀌었고, 렌더된 오선보도 이제 사진과 똑같이
  16분음표 이중 빔으로 그려진다(스크린샷으로 확인).
- 회귀 테스트 12개 파일(rhythm 19+8·score 86·geom 13+8·accid 32·chord 28·layout 28·
  live 21·mix 13·nokey 62·photo 20·sync 32·voicing 23) 한 파일씩 새로 열어 확인,
  **전부 통과**.

### APP_VER "74" → "75"

## 다음 세션이 이어받을 것
1. **이웃 음표 잉크가 겹쳐 보여 32분음표(bands=3)로 잘못 읽히는 음 2개.** 같은 시스템
   (「하나님의 부르심」1단)의 다른 두 음(x≈1227, x≈1392)이 사진에는 명확히 double-beam
   (16분음표)으로 보이는데 `bands=3`으로 나온다. 실측: 그 음의 기둥 옆 잉크 값이 정상
   빔의 거의 2배(side~185 vs 정상~100)로 찍혀, 옆 음표의 빔까지 같이 담는 것으로
   보인다(확정은 아님 — 다음 세션이 `__DURDBG`로 더 파 볼 것). 지금은 `sure:false`로
   안전하게 빠져 있어 급하지 않다 — CLAUDE.md 80번 항목 끝의 "남은 일" 참고.
2. v74가 남겼던 "사용자가 며칠 써 보고 여전히 이상한 악기가 있는지 확인" — 아직 답 없음.
3. (여러 세션째 보류 중) oemer/오프라인 전환 방향 조사 — 이번에도 안 건드림. 이어받으려면
   CLAUDE.md 71·76번 항목과 `research/omr-pitch/`를 먼저 읽을 것.

## 참고
- 사용자 요청으로 문맥이 600k 토큰을 넘으면 즉시 알리고 이어받기 요약을 준 뒤 `/compact`
  권장 (메모리: `compact-at-600k.md`)
- 발행 전 `_tmp*` 파일이 남아있지 않은지 항상 확인 후 커밋 — 이번 세션도 확인함(지움).
- 이번 세션은 `S.orig`(앱 저장분)에 사진이 없어서(브라우저 프로필이 새로 시작됨) 사용자가
  `악보/` 폴더에 둔 원본 파일을 직접 콘티 폴더에 `_tmp1.png`로 복사해 재현했다. 다음에도
  이 사진으로 재현하려면 같은 방법을 쓰면 된다(파일: `악보/하나님의 부르심 A코드.png`).
