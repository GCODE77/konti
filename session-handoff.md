# session-handoff.md — 콘티(konti) v77 인계 문서

## ★ v77에서 이어서 한 것 (같은 세션, 사용자가 실사용 중 이어서 신고)

사용자 신고 둘: *"모든 낮은시는 다 그렇다! 너가 만든게 낮은시!"*(= 음표 자리가 안
보인다) / *"2마디에 박자 원본악보랑 다른데 박자 개선해보자!"*. 둘 다 고쳤다.
자세한 원인·실측은 **CLAUDE.md 82번 항목**에 전부 적어 두었다. 요약만:

1. **음표 머리가 한 칸의 64% 밖에 안 됐다** — 조판 규칙은 "머리 높이 = 정확히 한 칸"이다.
   그래서 칸에 놓인 음이 위아래 줄에 안 닿아 흰 틈이 생겼고, 덧줄 하나에 매달린 낮은
   시(B3)는 라(A3)와 구별이 안 됐다. 두 렌더 모두 머리를 키우고(돌림 -18°를 감안해
   세로 반지름이 GAP 과 같아지도록), 기둥 붙는 자리·점·덧줄 길이를 같이 옮겼다.
2. **줄 위에 놓인 속이 찬 머리가 줄 지우기로 둘로 쪼개져 사라졌다**(42번 항목의 반대 얼굴).
   조각 둘을 붙여 보고, 갈라진 틈이 실제 줄 자리일 때만 받는다.
3. **`bigW` 구제 규칙이 줄 토막을 빔으로 셌다** — 문턱 0.35칸 → 0.8칸. (이 규칙은
   v73·v75·v76·v77 네 판 연속 말썽이다. 다음엔 폭 말고 좌우 대칭을 볼 것.)
4. **줄바꿈으로 갈린 마디를 4박으로 늘리고 있었다** — 합이 박 단위로 떨어지면 조각으로 본다.

이어서(사용자: *"남은 것까지 고치고 한번에"*) 7단 전부를 파서 여섯 가지를 더 고쳤다 —
8분쉼표(줄 토막 때문에 머리로 오인) · 마디선(줄 자리에서 끊김) · **빔 토막**을 머리로 읽음 ·
8·16·16 첫 음의 가짜 층 · 긴 기둥 · 이음줄로 넓어진 머리의 기둥. 상세는 CLAUDE.md 82번 ⑤~⑩.
**이전 판(51ac62a)을 `_tmp_old.html` 로 띄워 7단 전부 한 줄씩 대조했고, 달라진 음은 전부
사진으로 확인했다** — 놓친 음 7→0, 없는 음 5→0, 1~4단 8마디 전부 정답.

회귀 테스트 12개 파일 393개 전부 통과. 다음에 볼 곳: **곡 중간 박자표 숫자가 음으로 읽힘**
(5단 4/4, 72번) · **셋잇단음표**(6·7단, 75번) · **4분쉼표**(68번).

★ 이번에 만든 A/B 방법을 다음에도 쓸 것: `git show HEAD:index.html > _tmp_old.html` 후
두 페이지에서 같은 사진의 `staffGeom(parts[i].url).notes` 를 찍어 비교 → 달라진 음만 확대해
본다. 한 판에서 여러 규칙을 바꾸면 한 규칙이 다른 규칙이 막던 것을 되살리는 일이 실제로
세 번 있었다(♯ · 4분쉼표 · 2분음표 음높이) — 이 대조가 아니면 못 잡았다.

---

# (v76 인계 내용)

작성일: 2026-09-23. v75가 남긴 "다음 세션이 이어받을 것 1번"(이웃 음표 잉크가 겹쳐
보여 32분음표로 잘못 읽히는 유령 음)을, 사용자가 실사용 중 신고한 것을 계기로 이번
세션에서 실제로 고쳤다. 아래에 무엇을 왜 이렇게 고쳤는지 남긴다.

마지막 커밋: v75(974b904)까지. **이번 세션의 코드 변경(v76, 아래 항목)은 아직 커밋
전이다** — 사용자 확인 후 커밋할 것.

## ★★★★★★★ 이번 세션 — 유령 32분음표를 "이웃 문맥"으로 고쳤다 (v76)

### 계기

사용자가 「하나님의 부르심(로마서 11:29 손경민 작곡)」을 앱으로 직접 인식시켜 보고
스크린샷과 함께 신고했다 — *"빨간 첫 네모박스를 보면 실제 원본악보와 박자가 안맞다!
박자 개선해라."* 신고한 마디를 앱의 실제 경로(`prepImage`→`splitSystems`→`staffGeom`)로
재현하고, 검출한 음표·마디선 x좌표를 사진 위에 색선으로 덧그려 원본과 픽셀 대 픽셀로
맞대 봤다(재현 스크립트는 아래 "다음에 이어받으려면" 참고).

### 무엇이 문제였나

x=1131(F4)+x=1227(E4)와 x=1392(F4)+x=1489(G4), 두 쌍 모두 사진에서 **이중 빔으로
뚜렷하게 이어진 16분음표 짝**인데(확대해서 직접 확인함), 각 쌍의 뒷음(1227, 1392)만
`bands=3`으로 잘못 세여 32분음표(0.125박)로 읽히고 있었다. 이건 v75가 이미 원인을
확정하고 되돌린 바로 그 버그다 — "이웃 음표의 빔 잉크가 스캔 창에 같이 담긴다"는
원인은 맞지만, "크기(hr값)로 진짜 짧은 2차 빔과 못 가른다"는 이유로 감지기 자체를
고치는 건 포기했었다(v75 세션, `git log` 974b904).

### 어떻게 고쳤나 — 감지기를 안 건드리고 문맥 규칙을 추가했다

`staffGeom`이 한 시스템의 음표를 다 읽은 직후(`res` 배열이 완성된 시점, `durSure`
계산 **전**)에 새 규칙을 넣었다: `sure:false`이고 `beats===0.125`인 음이, 바로 옆
음표(간격이 그 시스템 음표 사이 간격의 중앙값×1.8 이내)가 `sure:true`이고
`beats===0.25`이면, 그 값을 그대로 물려받는다(`beats=0.25, sure=true`).

- 처음엔 거리 문턱을 오선 간격(`sp`)의 1.4배로 뒀는데 **전혀 안 걸렸다** — 이 악보의
  음표 사이 실제 간격은 `sp`의 4~6배였다(조판마다 다르다, CLAUDE.md 66번 항목).
  그 시스템 자신의 음표 간격 중앙값으로 바꾸니 바로 걸렸다.
- **이미 `sure:true`인 음(진짜 16분음표, 예 x=511·1966)에는 이 규칙이 원리적으로 안
  걸린다** — 감지 경로(headDur의 빔 겹수 세기) 자체를 전혀 안 건드렸기 때문이다.
  v75가 "크기 기준을 손보면 멀쩡한 음 2개가 같이 깨진다"고 확인해 둔 부작용이 이
  접근에는 생기지 않는다(코드는 `index.html`의 `staffGeom` 안, `if(res.length<3)return
  null;` 바로 다음 블록).

### 부수 효과 — durSure가 시스템 전체 단위였다

`durSure=res.every(n=>n.sure)`는 **그 시스템의 모든 음이 sure일 때만** 참이고,
`fitBeatsToBars`는 이 플래그 하나를 그 시스템의 **모든 마디에** 그대로 물려 쓴다. 즉
음 하나가 못 미더우면 아무 상관 없는 다른 마디까지 잰 길이를 버리고 간격-추정으로
다시 짜고 있었다. 이번 고침으로 이 곡 7단 전부가 `durSure:true`·못 미더운 음 0개가
됐다(고치기 전 최소 2개 단이 `sure:false` 음을 갖고 있었다).

### 실측

앱 실제 경로, 이 사진 1단: 고치기 전 `… F4:0.25 E4:0.125(unsure) E4:0.5 F4:0.125(unsure)
G4:0.25 …`(마디 합 3.75박) → 고친 뒤 `… F4:0.25 E4:0.25(sure) E4:0.5 F4:0.25(sure)
G4:0.25 …`(마디 합 4.0에 훨씬 가까움, `durSure:true`). 이 사진 7단 전부 재확인:
`durSure` 전부 true·못 미더운 음 0개(고치기 전엔 최소 2개 단이 있었다).

### 회귀 테스트

12개 파일(rhythm 27·score 86·geom 21·accid 32·chord 28·layout 28·live 21·mix 13·
nokey 62·photo 20·sync 32·voicing 23) 전부 한 파일씩 새로 열어 확인, **전부 통과**.
합성 테스트로는 이 버그를 재현하지 못한다(30번 항목과 같은 이유 — 실제 스캔의 잉크
번짐이 필요하다). 실제 사진으로만 검증했다.

## 아직 못 잡은 것 — 두 번째 신고

같은 대화에서 사용자가 이어서 신고했다 — *"실제로 음표, 음위치를 보면은 여백이
있으니 시인지, 라인지 보는사람도 헷갈린다. 명확히 시의 동그라미와 줄이 여백없이
끝자락에 붙어있다. 이런것도 개선해라."* 스크린샷에서 가리킨 음은 "…부르심이라"의
마지막 음(오렌지색 재생 표시, 패널 오른쪽 끝에 잘려 있었다) 근처로 보인다.

`parts[1]`(둘째 시스템)에서 x=1883의 B4(step4, 짝수=줄 위 음, sure:true)가 후보로
보이지만, 바로 앞 음(x=1859 D4)과 24px밖에 안 떨어져 있어(이 시스템 다른 음표 간격의
약 1/4) 이게 정말 사용자가 가리킨 음인지 사진과 맞대 확인하지 못한 채 세션이 끝났다.
**다음 세션은 시작하자마자**: (a) 사용자에게 정확한 마디 번호를 묻거나 화면을 다시
받고, (b) 그 음을 `__sys0canvas`에 색선으로 표시해 원본 사진과 대조할 것. "여백"
표현으로 보아 픽셀 검출 문제라기보다 **SVG 렌더링에서 줄 위 음(on-line note)의
notehead가 오선 줄에서 살짝 띄워져 그려지는** 문제일 가능성이 있다(`renderStaff`의
notehead 그리기 부분을 먼저 볼 것) — 확인 전 추측이니 코드를 먼저 읽고 판단할 것.

## 재현 스크립트 (다음 세션이 바로 쓸 수 있게 남긴다)

```js
// 1) 사진을 콘티 폴더에 _tmp1.png 로 복사, 로컬 서버로 열기(python -m http.server, 포트는
//    .claude/launch.json 의 konti 항목 참고)
// 2) 콘솔에서:
const blob=await (await fetch('_tmp1.png')).blob();
const ph=await prepImage(new File([blob],'_tmp1.png',{type:'image/png'}));
const parts=await splitSystems(ph.hiUrl||ph.url);
const g=await staffGeom(parts[0].url);   // 시스템 번호는 필요에 따라 바꿀 것
// g.notes 에서 x, name, beats, sure 를 확인. 사진과 맞대 보려면 아래로 캔버스에 겹쳐 그린다:
const img=await new Promise((res,rej)=>{const im=new Image();im.onload=()=>res(im);im.onerror=rej;im.src=parts[0].url;});
const c=document.createElement('canvas'); c.width=img.width; c.height=img.height;
c.getContext('2d').drawImage(img,0,0);
for(const n of g.notes){ const ctx=c.getContext('2d'); ctx.strokeStyle=n.sure?'lime':'red';
  ctx.beginPath(); ctx.moveTo(n.x,0); ctx.lineTo(n.x,c.height); ctx.stroke(); }
document.body.innerHTML='<img src="'+c.toDataURL('image/png')+'">'; // 스크린샷으로 확인
```

## 참고
- 사용자 요청으로 문맥이 600k 토큰을 넘으면 즉시 알리고 이어받기 요약을 준 뒤 `/compact`
  권장 (메모리: `compact-at-600k.md`)
- 발행 전 `_tmp*` 파일이 남아있지 않은지 항상 확인 후 커밋.
- v74가 남겼던 "사용자가 며칠 써 보고 여전히 이상한 악기가 있는지 확인" — 아직 답 없음.
- (여러 세션째 보류 중) oemer/오프라인 전환 방향 조사 — 이번에도 안 건드림(사용자가
  실사용 버그를 먼저 신고해 그쪽을 처리했다). 이어받으려면 CLAUDE.md 71·76번 항목과
  `research/omr-pitch/`를 먼저 읽을 것.
