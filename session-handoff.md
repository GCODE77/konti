# session-handoff.md — 콘티(konti) v75 인계 문서

작성일: 2026-09-23 (v75 인계 문서를 이어받은 새 세션. v75가 남긴 "다음 세션이 이어받을
것 1번" — 이웃 음표 잉크가 겹쳐 보여 32분음표(bands=3)로 잘못 읽히는 음 2개 — 를 이번
세션에서 `__DURDBG`로 직접 파 보고 원인을 확정했다. 고치려고도 해 봤지만 **되돌렸다** —
아래에 그 이유를 자세히 남긴다.)

마지막 커밋: v75(5c233de)까지 커밋·푸시 없이 로컬에 있었으나, 이번 세션 시작에 사용자
확인 후 커밋했다. **이번 세션은 코드를 바꾸지 않았다**(시도했다가 되돌림) — 커밋할 것 없음.

## ★★★★★★★ 이번 세션 — v75가 남긴 "다음 할 일 1번"을 파 보고, 고치지 않기로 결정했다

### 무엇을 발견했나

「하나님의 부르심 A코드.png」1단(x≈1227 E4, x≈1392 F4, 둘 다 `bands=3`으로 잘못 읽혀
`sure:false`)을 `window.__DURDBG`로 실측했다. 두 진짜 빔 층(각각 hr값 ~103·~185, 아주
굵고 명백한 진짜 빔) **사이의 틈**에서, 오선 셋째 줄이 지나가는 정확한 자리(`isL`)에
작은 잉크 잔상(hr값 18·19)이 하나 더 있었고, 이게 "폭이 넓으면 줄 위여도 인정한다"는
구제 규칙(v57, `bigW=need+sp*0.35≈16.5`)을 가까스로 넘겨(18/19 > 16.5) 세 번째 층으로
잘못 세였다. 이 잔상은 지워진 오선 줄이 완전히 지워지지 않고 남은 부스러기로 보인다.

### 왜 못 고쳤나 — 핵심 발견

"진짜 빔 층은 크기가 서로 비슷하다(hr값이 같은 자리수)"는 가정으로 구제 규칙을
손봐 봤다 — 이미 확인된 "줄 밖에서도 나온 진짜 층"이 있으면, 줄에만 걸치는 층은
그 진짜 층 중 가장 약한 것의 35% 는 돼야 인정하도록. 이 시스템의 x=1227·x=1392는
실제로 18/103≈0.17, 19/103≈0.18로 깨끗하게 걸러졌다.

**그런데 같은 시스템의 다른 멀쩡한 음 둘(x=511, x=1966 — 원래도 정확했던 16분음표)이
같이 깨졌다.** 이 두 음도 "줄에만 걸치는 약한 층"을 하나씩 갖고 있는데, 그 값이
x=511은 **40**, x=1966은 **17**이었다 — x=1966의 17은 스푸리어스였던 1227의 18과
**거의 구별이 안 되는 크기**다. 그런데 이 17은 가짜가 아니라 **진짜** 2층(16분음표)을
만드는 데 꼭 필요한 층이었다(빼면 bands가 1로 떨어져 오히려 틀려진다).

원인: 1차 빔(여러 음을 길게 잇는 빔)과 2차 빔(인접한 두 16분음표만 짧게 잇는 빔)은
**원래 hr(수평 잉크 길이)가 다르다** — 2차 빔은 원래 짧게만 이어서 값이 작다. 그래서
"진짜 층끼리는 크기가 비슷하다"는 전제 자체가 틀렸다. 크기만으로는 "지워진 줄
잔상"(x=1227의 18)과 "진짜지만 짧은 2차 빔"(x=1966의 17)을 가를 수 없다 — 사실상
같은 숫자다.

**결론: 되돌렸다.** 고치면 확인된 버그(2개, 이미 `sure:false`로 안전하게 빠져 있어
피해가 없음)를 없애는 대신, 멀쩡했던 음 2개를 새로 깨뜨린다. CLAUDE.md 가 여러 번
적어 둔 원칙("고치려다 잃는 쪽이 늘 더 나쁘다")과 정확히 같은 상황이라 판단해 코드를
원래대로 되돌렸다(`git diff` 로 v75 커밋과 완전히 같음을 확인함). index.html·CLAUDE.md
는 이번 세션에서 **전혀 바뀌지 않았다**.

### 다음에 이어받으려면

이 문제를 다시 풀려면 "hr 크기"가 아닌 **다른 신호**가 필요하다. 이번에 시도하고
버린 방향(참고만 할 것, 다시 시도하지 말 것): 크기 비율 비교. 아직 안 시도해 본 방향:
- 잔상의 **모양**(대칭성, 폭의 분포)이 진짜 빔 끄트머리와 다른지 — 이번엔 `dy=±1`
  스무딩 때문에 둘 다 "같은 값 3번 반복"으로 나와 구별이 안 됐다. 스무딩을 걷고
  1px 단위로 다시 봐야 할 수도 있다.
- **틈의 폭**(진짜 빔 사이 틈 vs 잔상-빔 사이 틈)이 다른지.
- oemer(독립 CNN, v76에서 이미 한 번 대조해 본 도구)로 이 시스템만 다시 대조해서
  x=1227·1392·511·1966 네 음의 정답을 教차검증하는 것도 방법이다.
- **디버그 재현 스크립트**(다음 세션이 바로 쓸 수 있게 남긴다):
  ```js
  // 1) 사진을 콘티 폴더에 _tmp1.png 로 복사, 로컬 서버로 열기(예: python -m http.server)
  // 2) 콘솔에서:
  window.__DURDBG=[];
  const blob=await (await fetch('_tmp1.png')).blob();
  const ph=await prepImage(new File([blob],'_tmp1.png',{type:'image/png'}));
  const parts=await splitSystems(ph.hiUrl||ph.url);
  const g=await staffGeom(parts[0].url);   // 1단
  // g.notes 에서 x≈1227/1392(스푸리어스) 와 x≈511/1966(진짜) 를 __DURDBG 에서 비교
  ```

## 참고
- 사용자 요청으로 문맥이 600k 토큰을 넘으면 즉시 알리고 이어받기 요약을 준 뒤 `/compact`
  권장 (메모리: `compact-at-600k.md`)
- 발행 전 `_tmp*` 파일이 남아있지 않은지 항상 확인 후 커밋 — 이번 세션도 확인함(지움).
- v74가 남겼던 "사용자가 며칠 써 보고 여전히 이상한 악기가 있는지 확인" — 아직 답 없음.
- (여러 세션째 보류 중) oemer/오프라인 전환 방향 조사 — 이번에도 안 건드림. 이어받으려면
  CLAUDE.md 71·76번 항목과 `research/omr-pitch/`를 먼저 읽을 것.
